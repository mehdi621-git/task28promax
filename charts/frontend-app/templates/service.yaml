apiVersion: v1
kind: Service
metadata:
  name: frontend-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: frontend-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
